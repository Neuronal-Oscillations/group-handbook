# Command Line Functions for Neuronal Oscillations Group (nog_ functions)

Download the repository and add group-handbook/code/hpc/bin to `$PATH` in `~/.bashrc`. Also make the files executable using `chmod +x`

When using `bash group-handbook/code/hpc/setup_sysenv.sh` this is done automatically.
